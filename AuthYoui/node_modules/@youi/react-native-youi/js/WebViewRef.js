'use strict'

import PropTypes from 'prop-types';
import React from 'react'
import {
  NativeModules,
  requireNativeComponent,
  UIManager,
  View
} from 'react-native'

import WebView from './private/WebView.ios.js'

export default class WebViewRef extends React.Component {
    render() {
        return (
            <WebView
                {...this.props}
            />
        )
    }
}