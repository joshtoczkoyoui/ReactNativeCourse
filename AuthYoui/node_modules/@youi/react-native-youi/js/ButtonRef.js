'use strict'

import PropTypes from 'prop-types';
import React, {Component} from 'react'
import {
  requireNativeComponent,
  UIManager
} from 'react-native'

import { FocusableRef } from "./private/FocusableRef.js"
import { TimelineHolder } from "./private/TimelineHolder.js"

let _ButtonRef = requireNativeComponent('YiPushButtonView')

class ButtonRef extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <_ButtonRef
        {...this.props}
      />
    );
  }
}

export default TimelineHolder(FocusableRef(ButtonRef));
