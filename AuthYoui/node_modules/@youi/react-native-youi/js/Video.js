'use strict'

import PropTypes from 'prop-types';
import React, {Component} from 'react';
import {
  requireNativeComponent,
  UIManager,
  ViewPropTypes,
} from 'react-native'

const _Video = requireNativeComponent('Video', Video, {
  nativeOnly: {
    src: true,
    seek: true,
  },
});

import { VideoHolder } from "./private/VideoHolder.js"

class Video extends React.Component {

  render() {
    return (
      <_Video
        {...this.props}
      />
    );
  }
}

export default VideoHolder(Video);



