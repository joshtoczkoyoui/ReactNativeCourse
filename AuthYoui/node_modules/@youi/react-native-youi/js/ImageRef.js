'use strict'

import PropTypes from 'prop-types';
import React, {Component} from 'react'
import {
  requireNativeComponent,
  UIManager
} from 'react-native'

import { TimelineHolder } from "./private/TimelineHolder.js"

let _ImageRef = requireNativeComponent('YiImageView')

class ImageRef extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <_ImageRef
        {...this.props}
      />
    );
  }
}

export default TimelineHolder(ImageRef);
