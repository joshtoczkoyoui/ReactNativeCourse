'use strict'

import PropTypes from 'prop-types';
import React, {Component} from 'react'
import {
  requireNativeComponent,
  UIManager,
} from 'react-native'

import { VideoHolder } from "./private/VideoHolder.js"

let _VideoRef = requireNativeComponent('VideoRef')

class VideoRef extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <_VideoRef
        {...this.props}
      />
    );
  }
}

export default VideoHolder(VideoRef);