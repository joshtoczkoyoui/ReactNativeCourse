import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Button = () => {
    return (
        <TouchableOpacity>
            <Text>CLICK ME</Text>
        </TouchableOpacity>
    );
};

export default Button;
